version: '3.8'
services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    ports:
      - "4200:4200"
    networks:
      - devnetwork

networks:
  devnetwork:
    external: true
